\documentclass{ximera}

\title{Jacobians and Transformations}
\author{Zack Reed}

\begin{document}
\begin{abstract}
We explore how coordinate transformations stretch and skew space, building geometric intuition for the Jacobian as a measure of local area and volume distortion --- starting with polar, cylindrical, and spherical coordinates before moving to general transformations.
\end{abstract}
\maketitle

%----------------------------------------------------------------------
\section*{How Much Do Coordinate Transformations Skew Space?}
%----------------------------------------------------------------------

When we switch from Cartesian coordinates to polar, cylindrical, spherical, or other more general cooridnates, we are applying a \emph{transformation}: a rule that maps one coordinate system to another.  

For polar coordinates, the transformation was from the variables $(r,\theta)$ to the variables $(x,y)$, given by the formulas

\[x = r\cos\theta, \qquad y = r\sin\theta.\]

We measured the area of a small region in polar space by the area $dA = r\,dr\,d\theta$. 


\begin{center}
\begin{tikzpicture}[scale=1.05]

%=== LEFT: parameter space (r,theta) rectangle ===
\begin{scope}[xshift=-1cm]
  \draw[->](-0.25,0)--(3.6,0) node[right]{$r$};
  \draw[->](0,-0.25)--(0,3.2) node[above]{$\theta$};

  % grid lines
  \draw[gray!30,thin] (0,0) grid[step=0.6] (3.4,3.0);

  % highlight one small rectangle
  \def\ro{1.8}  \def\tho{1.2}  \def\dr{0.6}  \def\dth{0.6}
  \fill[blue!20] (\ro,\tho) rectangle (\ro+\dr,\tho+\dth);
  \draw[blue!70!black,very thick] (\ro,\tho) rectangle (\ro+\dr,\tho+\dth);

  % dimension labels
  \draw[<->,orange!80!black,thick] (\ro,\tho-0.2) -- (\ro+\dr,\tho-0.2)
        node[midway,below]{$d r$};
  \draw[<->,orange!80!black,thick] (\ro+\dr+0.15,\tho) -- (\ro+\dr+0.15,\tho+\dth)
        node[midway,right]{$r\,d\theta$};

  % area label
  % \node[blue!70!black,font=\small] at (\ro+\dr/2,\tho+\dth/2)
  %       {$\Delta r\,\Delta\theta$};

  % annotation box
  \draw[green!50!black,thick,rounded corners] (-.8,-0.5) rectangle (4.2,-0.1);
  \node[green!50!black,font=\small,align=center] at (1.7,-0.3)
        {Area just multiplies sides};

  \node[font=\small,align=center,below] at (1.7,-0.55)
        {\textbf{Parameter space} $(r,\theta)$};
\end{scope}

%=== ARROW ===
\begin{scope}[xshift=4.0cm,yshift=1cm]
  \draw[->,very thick,gray!60](0,0)--(1.2,0) node[midway,above,font=\small]{$T$};
  \node[gray!60,font=\scriptsize,align=center] at (0.6,-0.3){$x=r\cos\theta$\\$y=r\sin\theta$};
\end{scope}

%=== RIGHT: coordinate space (x,y) curved patch ===
\begin{scope}[xshift=6.4cm]
  \draw[->](-0.25,0)--(3.6,0) node[right]{$x$};
  \draw[->](0,-0.25)--(0,3.2) node[above]{$y$};

  % Draw several annular sectors to show the non-uniform grid
  \foreach \ri/\rn in {0.55/1.15, 1.15/1.75, 1.75/2.35, 2.35/2.95} {
    \foreach \ta/\tb in {10/37, 37/64, 64/91, 91/118} {
      \pgfmathsetmacro{\cc}{(\ri+\rn)/2*15}
      \fill[blue!\cc!white,opacity=0.35]
        ({\ri*cos(\ta)},{\ri*sin(\ta)})
        -- ({\rn*cos(\ta)},{\rn*sin(\ta)})
        arc[start angle=\ta, end angle=\tb, radius=\rn]
        -- ({\ri*cos(\tb)},{\ri*sin(\tb)})
        arc[start angle=\tb, end angle=\ta, radius=\ri]
        -- cycle;
      \draw[blue!60!black,thin]
        ({\ri*cos(\ta)},{\ri*sin(\ta)})
        -- ({\rn*cos(\ta)},{\rn*sin(\ta)})
        arc[start angle=\ta, end angle=\tb, radius=\rn]
        -- ({\ri*cos(\tb)},{\ri*sin(\tb)})
        arc[start angle=\tb, end angle=\ta, radius=\ri]
        -- cycle;
    }
  }

  % Highlight one specific patch: r=1.75..2.35, theta=37..64 deg
  \fill[blue!25]
    ({1.75*cos(37)},{1.75*sin(37)})
    -- ({2.35*cos(37)},{2.35*sin(37)})
    arc[start angle=37, end angle=64, radius=2.35]
    -- ({1.75*cos(64)},{1.75*sin(64)})
    arc[start angle=64, end angle=37, radius=1.75]
    -- cycle;
  \draw[blue!70!black,very thick]
    ({1.75*cos(37)},{1.75*sin(37)})
    -- ({2.35*cos(37)},{2.35*sin(37)})
    arc[start angle=37, end angle=64, radius=2.35]
    -- ({1.75*cos(64)},{1.75*sin(64)})
    arc[start angle=64, end angle=37, radius=1.75]
    -- cycle;

  % Label radial side
  \pgfmathsetmacro{\rmid}{(1.75+2.35)/2}
  \pgfmathsetmacro{\tmid}{(37+64)/2}
  \draw[<->,orange!80!black,thick]
    ({1.77*cos(36)},{1.77*sin(36)}) -- ({2.33*cos(36)},{2.33*sin(36)})
    node[midway,below,font=\small]{$d r$};

  % Label arc side (outer arc at r=2.35)#medium font
  \node[orange!80!black,font=\small] at ({2.75*cos(\tmid)},{2.75*sin(\tmid)})
        {$r\,d\theta$};
  \draw[orange!70!black,very thick,<->]
    ({2.4*cos(37)},{2.4*sin(37)}) arc[start angle=37,end angle=64,radius=2.4];

  % annotation box below
  \draw[red!60!black,thick,rounded corners] (-.35,-0.55) rectangle (3.75,-.15);
  \node[red!60!black,font=\small,align=center] at (1.7,-0.35)
        {Arc $= r\,d\theta$ so area $\approx r\,d r\,d\theta$};

  \node[font=\small,align=center,below] at (1.7,-0.6)
        {\textbf{Coordinate space} $(x,y)$};
\end{scope}

\end{tikzpicture}
\end{center}


In cylindrical coordinates, the transformation was from the variables $(r,\theta,z)$ to the variables $(x,y,z)$, given by the formulas

\[x = r\cos\theta, \qquad y = r\sin\theta, \qquad z = z.\]

We simply added a small height change $dz$ to the polar area, so the volume of a small box in cylindrical space was $dV = r\,dr\,d\theta\,dz$.

\begin{center}
\begin{tikzpicture}[scale=0.95]

%=== LEFT: parameter space (r, theta, z) rectangular box ===
\begin{scope}[xshift=0cm]
  % Draw as a simple 3D oblique box
  % using a pseudo-3D projection: x-axis goes right, y-axis goes up, z-axis goes upper-right
  % basis: e_r = (1,0), e_theta = (0,1), e_z = (0.45, 0.45)
  \def\er{(1,0)}  \def\et{(0,1)}  \def\ez{(0.45,0.45)}
  \def\W{1.4}  \def\H{1.1}  \def\D{0.7}
  % The eight corners (origin + combinations)
  % O = (0,0), A=W*er, B=H*et, C=D*ez, AB=A+B, AC=A+C, BC=B+C, ABC=A+B+C
  \coordinate (O) at (0,0);
  \coordinate (A) at (\W,0);
  \coordinate (B) at (0,\H);
  \coordinate (C) at ({\D*0.45},{\D*0.45});
  \coordinate (AB) at (\W,\H);
  \coordinate (AC) at ({\W+\D*0.45},{\D*0.45});
  \coordinate (BC) at ({\D*0.45},{\H+\D*0.45});
  \coordinate (ABC) at ({\W+\D*0.45},{\H+\D*0.45});

  % Fill faces
  \fill[blue!15] (O)--(A)--(AB)--(B)--cycle;
  \fill[blue!25] (A)--(AC)--(ABC)--(AB)--cycle;
  \fill[blue!10] (B)--(AB)--(ABC)--(BC)--cycle;

  % Draw edges
  \draw[blue!70!black,thick] (O)--(A)--(AB)--(B)--cycle;
  \draw[blue!70!black,thick] (A)--(AC)--(ABC)--(AB);
  \draw[blue!70!black,thick] (B)--(BC)--(ABC);
  \draw[blue!70!black,dashed] (O)--(C)--(AC);
  \draw[blue!70!black,dashed] (C)--(BC);

  % Dimension labels
  \draw[<->,orange!80!black,thick] (0,-0.25)--(\W,-0.25) node[midway,below]{$d r$};
  \draw[<->,orange!80!black,thick] (-0.25,0)--(-0.25,\H) node[midway,left]{$d z$};
  \draw[<->,orange!80!black,thick] ({\W+0.15},{0.08*\D})--({\W+\D*0.45+0.15},{\D*0.45+0.08*\D})
        node[midway,right,font=\small]{$r \,d\theta$};

  % Volume annotation
  \node[blue!70!black,font=\small,align=center] at ({-1.5},{\H/2-.5}) {Volume};
  \node[blue!70!black,font=\small,align=center] at ({-1.5},{-0.3}) {$= r\,d r\cdot\,d\theta\cdot\,d z$};

  \draw[green!50!black,rounded corners,thick] (-.1,-.7) rectangle ({\W+\D*0.45+0.25},{\H+\D*0.45+0.2});
  \node[green!50!black,font=\small] at ({\W/2},-0.9) {sides simply multiply};
  \node[font=\small,align=center,below] at ({(\W+\D*0.45)/2},-1.15)
        {\textbf{Parameter space} $(r,\theta,z)$};
\end{scope}

%=== ARROW ===
\begin{scope}[xshift=3.8cm]
  \draw[->,very thick,gray!60](0,0.55)--(1.4,0.55)
    node[midway,above,align=center,font=\small]{Cylindrical\\Transformation};

%=== RIGHT: coordinate space (x,y,z) curved prism ===
\begin{scope}[xshift=3.5cm, yshift=-0.3cm]
  % Draw using standard 3D oblique: x goes right, y goes upper-right, z goes up
  % A curved prism: bottom face is a polar wedge, extruded up by Delta z
  % r_in=0.7, r_out=1.5, theta_a=25, theta_b=55, z_bot=0, z_top=1.1
  \def\rI{0.7}  \def\rO{1.5}  \def\thA{25}  \def\thB{55}  \def\zt{1.15}
  % Project 3D (x,y,z) -> 2D: px = x + 0.4*y, py = z + 0.25*y
  % bottom face corners: 4 points at z=0
  % IL = inner-left, IR = inner-right, OL = outer-left, OR = outer-right
  \pgfmathsetmacro{\ILx}{\rI*cos(\thA)}  \pgfmathsetmacro{\ILy}{\rI*sin(\thA)}
  \pgfmathsetmacro{\IRx}{\rI*cos(\thB)}  \pgfmathsetmacro{\IRy}{\rI*sin(\thB)}
  \pgfmathsetmacro{\OLx}{\rO*cos(\thA)}  \pgfmathsetmacro{\OLy}{\rO*sin(\thA)}
  \pgfmathsetmacro{\ORx}{\rO*cos(\thB)}  \pgfmathsetmacro{\ORy}{\rO*sin(\thB)}
  % 2D projected coords (px, py) = (x + 0.38*y, 0 + 0.28*y) for z=0
  % and (x + 0.38*y, zt + 0.28*y) for z=zt
  \pgfmathsetmacro{\pILx}{\ILx+0.38*\ILy}  \pgfmathsetmacro{\pILy}{0.28*\ILy}
  \pgfmathsetmacro{\pIRx}{\IRx+0.38*\IRy}  \pgfmathsetmacro{\pIRy}{0.28*\IRy}
  \pgfmathsetmacro{\pOLx}{\OLx+0.38*\OLy}  \pgfmathsetmacro{\pOLy}{0.28*\OLy}
  \pgfmathsetmacro{\pORx}{\ORx+0.38*\ORy}  \pgfmathsetmacro{\pORy}{0.28*\ORy}
  % Top versions (add zt to z component -> add zt to py)
  \pgfmathsetmacro{\tILy}{\pILy+\zt}  \pgfmathsetmacro{\tIRy}{\pIRy+\zt}
  \pgfmathsetmacro{\tOLy}{\pOLy+\zt}  \pgfmathsetmacro{\tORy}{\pORy+\zt}

  % Back face (for visibility order)
  \fill[red!10]
    (\pOLx,\pOLy) -- (\pORx,\pORy) -- (\pORx,\tORy) -- (\pOLx,\tOLy) -- cycle;
  \fill[red!15]
    (\pILx,\pILy) -- (\pOLx,\pOLy) -- (\pOLx,\tOLy) -- (\pILx,\tILy) -- cycle;

  % Bottom curved face (approximate with straight lines)
  \fill[red!20]
    (\pILx,\pILy) -- (\pOLx,\pOLy) -- (\pORx,\pORy) -- (\pIRx,\pIRy) -- cycle;

  % Top curved face
  \fill[red!25]
    (\pILx,\tILy) -- (\pOLx,\tOLy) -- (\pORx,\tORy) -- (\pIRx,\tIRy) -- cycle;

  % Draw all edges
  \draw[red!70!black,thick]
    (\pILx,\pILy) -- (\pOLx,\pOLy) -- (\pORx,\pORy) -- (\pIRx,\pIRy) -- cycle;
  \draw[red!70!black,thick]
    (\pILx,\tILy) -- (\pOLx,\tOLy) -- (\pORx,\tORy) -- (\pIRx,\tIRy) -- cycle;
  \draw[red!70!black,thick] (\pILx,\pILy)--(\pILx,\tILy);
  \draw[red!70!black,thick] (\pIRx,\pIRy)--(\pIRx,\tIRy);
  \draw[red!70!black,thick] (\pOLx,\pOLy)--(\pOLx,\tOLy);
  \draw[red!70!black,thick] (\pORx,\pORy)--(\pORx,\tORy);

  % Dimension labels
  \pgfmathsetmacro{\rmid}{(\rI+\rO)/2}
  \pgfmathsetmacro{\tmid}{(\thA+\thB)/2}
  \pgfmathsetmacro{\pmx}{\rmid*cos(\thA)+0.38*\rmid*sin(\thA)}
  \pgfmathsetmacro{\pmy}{0.28*\rmid*sin(\thA)}
  \draw[<->,orange!80!black,thick]
    ({\pILx-0.1},{\pILy-0.22}) -- ({\pOLx-0.1},{\pOLy-0.22})
    node[midway,below,font=\small]{$d r$};

  % arc label
  \pgfmathsetmacro{\pARCx}{\rO*cos(\tmid)+0.38*\rO*sin(\tmid)+0.2}
  \pgfmathsetmacro{\pARCy}{0.28*\rO*sin(\tmid)}
  \node[orange!80!black,right,font=\small] at (\pARCx,\pARCy) {$r\,d\theta$};
  \draw[orange!80!black,->,thin] (\pARCx-0.18,\pARCy) -- (\pORx+0.05,\pORy+0.05);

  % height label
  \draw[<->,orange!80!black,thick]
    ({\pILx+0.18},{\pILy}) -- ({\pILx+0.18},{\tILy})
    node[midway,right,font=\small]{$d z$};

  % Bounding box (tight around prism only, mirroring green box on left)
  \draw[red!60!black,rounded corners,thick] (-0.1,-0.55) rectangle (1.85,{\zt+0.35});
  \node[font=\small,align=center,below] at (0.85,{-0.55})
        {\textbf{Coordinate space} $(x,y,z)$};

  % Volume annotation to the RIGHT (mirroring left side)
  \node[red!70!black,font=\small,align=center] at (2.65,{\zt/2-.7}) {Volume};
  \node[red!70!black,font=\small,align=center] at (3.65,{\zt/2-.95}) {$\approx r\cdot d r\cdot d\theta\cdot d z$};
\end{scope}

\end{scope}

\end{tikzpicture}
\end{center}

In spherical coordinates, the transformation was from the variables $(\rho,\phi,\theta)$ to the variables $(x,y,z)$, given by the formulas

\[x = \rho\sin\phi\cos\theta, \qquad y = \rho\sin\phi\sin\theta, \qquad z = \rho\cos\phi.\]

The geometry of the transformation gave us that the measure of a small volume wedge in spherical space was $dV = \rho^2\sin\phi\,d\rho\,d\phi\,d\theta$.

\begin{center}
\begin{tikzpicture}[x={(0.8cm,-0.4cm)}, y={(0.8cm,0.4cm)}, z={(0cm,1.1cm)}, scale=1.4]
% Sphere wireframe + one highlighted dV wedge element — no axes, minimal wireframe
\def\RS{2.0}

% --- Back hemisphere wireframe (thin dashed — more lines for rounder look) ---
\foreach \ph in {20,40,60,80,100,120,140,160} {
  \pgfmathsetmacro{\rr}{\RS*sin(\ph)}
  \pgfmathsetmacro{\zz}{\RS*cos(\ph)}
  \draw[blue!20!black,ultra thin,dashed,domain=90:270,smooth,samples=13,variable=\t]
    plot ({\rr*cos(\t)},{\rr*sin(\t)},\zz);
}
\foreach \th in {90,120,150,180,210,240,270} {
  \draw[blue!20!black,ultra thin,dashed,domain=0:180,smooth,samples=13,variable=\p]
    plot ({\RS*sin(\p)*cos(\th)},{\RS*sin(\p)*sin(\th)},{\RS*cos(\p)});
}

% Light fill on back hemisphere (very faint)
\foreach \ph in {0,40,...,160} {
  \pgfmathsetmacro{\phn}{\ph+40}
  \pgfmathsetmacro{\ra}{\RS*sin(\ph)} \pgfmathsetmacro{\za}{\RS*cos(\ph)}
  \pgfmathsetmacro{\rb}{\RS*sin(\phn)} \pgfmathsetmacro{\zb}{\RS*cos(\phn)}
  \foreach \th in {90,180,240} {
    \pgfmathsetmacro{\thn}{\th+60}
    \fill[blue!12!cyan,opacity=0.10]
      ({\ra*cos(\th)},{\ra*sin(\th)},\za)
      -- ({\rb*cos(\th)},{\rb*sin(\th)},\zb)
      -- ({\rb*cos(\thn)},{\rb*sin(\thn)},\zb)
      -- ({\ra*cos(\thn)},{\ra*sin(\thn)},\za) -- cycle;
  }
}

% --- Front hemisphere (thin lines only — more lines for rounder look) ---
\foreach \ph in {20,40,60,80,100,120,140,160} {
  \pgfmathsetmacro{\rr}{\RS*sin(\ph)}
  \pgfmathsetmacro{\zz}{\RS*cos(\ph)}
  \draw[blue!30!black,ultra thin,domain=-90:90,smooth,samples=13,variable=\t]
    plot ({\rr*cos(\t)},{\rr*sin(\t)},\zz);
}
\foreach \th in {-90,-60,-30,0,30,60,90} {
  \draw[blue!30!black,ultra thin,domain=0:180,smooth,samples=13,variable=\p]
    plot ({\RS*sin(\p)*cos(\th)},{\RS*sin(\p)*sin(\th)},{\RS*cos(\p)});
}

% Light fill on front hemisphere (gentle)
\foreach \ph in {0,40,...,160} {
  \pgfmathsetmacro{\phn}{\ph+40}
  \pgfmathsetmacro{\ra}{\RS*sin(\ph)} \pgfmathsetmacro{\za}{\RS*cos(\ph)}
  \pgfmathsetmacro{\rb}{\RS*sin(\phn)} \pgfmathsetmacro{\zb}{\RS*cos(\phn)}
  \foreach \th in {-90,-30,...,60} {
    \pgfmathsetmacro{\thn}{\th+60}
    \fill[blue!18!cyan,opacity=0.12]
      ({\ra*cos(\th)},{\ra*sin(\th)},\za)
      -- ({\rb*cos(\th)},{\rb*sin(\th)},\zb)
      -- ({\rb*cos(\thn)},{\rb*sin(\thn)},\zb)
      -- ({\ra*cos(\thn)},{\ra*sin(\thn)},\za) -- cycle;
  }
}

% --- Highlighted dV wedge element ---
% rho in [1.1,1.5], phi in [45,65 deg], theta in [15,50 deg]
\def\rI{1.1} \def\rO{1.5}
\def\phA{45} \def\phB{65}
\def\thA{15} \def\thB{50}

% 8 corner coordinates (pre-compute with helper macros)
% inner/outer, phi low/high, theta low/high: notation (rho, ph, th)
% We draw 6 faces as quads

% Face 1: inner sphere face (rho=rI, phi, theta sweep)
\fill[yellow!50,opacity=0.80]
  ({\rI*sin(\phA)*cos(\thA)},{\rI*sin(\phA)*sin(\thA)},{\rI*cos(\phA)})
  -- ({\rI*sin(\phB)*cos(\thA)},{\rI*sin(\phB)*sin(\thA)},{\rI*cos(\phB)})
  -- ({\rI*sin(\phB)*cos(\thB)},{\rI*sin(\phB)*sin(\thB)},{\rI*cos(\phB)})
  -- ({\rI*sin(\phA)*cos(\thB)},{\rI*sin(\phA)*sin(\thB)},{\rI*cos(\phA)}) -- cycle;

% Face 2: outer sphere face (rho=rO)
\fill[yellow!60,opacity=0.85]
  ({\rO*sin(\phA)*cos(\thA)},{\rO*sin(\phA)*sin(\thA)},{\rO*cos(\phA)})
  -- ({\rO*sin(\phB)*cos(\thA)},{\rO*sin(\phB)*sin(\thA)},{\rO*cos(\phB)})
  -- ({\rO*sin(\phB)*cos(\thB)},{\rO*sin(\phB)*sin(\thB)},{\rO*cos(\phB)})
  -- ({\rO*sin(\phA)*cos(\thB)},{\rO*sin(\phA)*sin(\thB)},{\rO*cos(\phA)}) -- cycle;

% Face 3: phi=phA (upper cone face)
\fill[yellow!40,opacity=0.75]
  ({\rI*sin(\phA)*cos(\thA)},{\rI*sin(\phA)*sin(\thA)},{\rI*cos(\phA)})
  -- ({\rO*sin(\phA)*cos(\thA)},{\rO*sin(\phA)*sin(\thA)},{\rO*cos(\phA)})
  -- ({\rO*sin(\phA)*cos(\thB)},{\rO*sin(\phA)*sin(\thB)},{\rO*cos(\phA)})
  -- ({\rI*sin(\phA)*cos(\thB)},{\rI*sin(\phA)*sin(\thB)},{\rI*cos(\phA)}) -- cycle;

% Face 4: phi=phB (lower cone face)
\fill[yellow!40,opacity=0.75]
  ({\rI*sin(\phB)*cos(\thA)},{\rI*sin(\phB)*sin(\thA)},{\rI*cos(\phB)})
  -- ({\rO*sin(\phB)*cos(\thA)},{\rO*sin(\phB)*sin(\thA)},{\rO*cos(\phB)})
  -- ({\rO*sin(\phB)*cos(\thB)},{\rO*sin(\phB)*sin(\thB)},{\rO*cos(\phB)})
  -- ({\rI*sin(\phB)*cos(\thB)},{\rI*sin(\phB)*sin(\thB)},{\rI*cos(\phB)}) -- cycle;

% Face 5: theta=thA (front meridian face)
\fill[yellow!55,opacity=0.82]
  ({\rI*sin(\phA)*cos(\thA)},{\rI*sin(\phA)*sin(\thA)},{\rI*cos(\phA)})
  -- ({\rI*sin(\phB)*cos(\thA)},{\rI*sin(\phB)*sin(\thA)},{\rI*cos(\phB)})
  -- ({\rO*sin(\phB)*cos(\thA)},{\rO*sin(\phB)*sin(\thA)},{\rO*cos(\phB)})
  -- ({\rO*sin(\phA)*cos(\thA)},{\rO*sin(\phA)*sin(\thA)},{\rO*cos(\phA)}) -- cycle;

% Face 6: theta=thB (back face of wedge -- draw at lower opacity)
\fill[yellow!45,opacity=0.65]
  ({\rI*sin(\phA)*cos(\thB)},{\rI*sin(\phA)*sin(\thB)},{\rI*cos(\phA)})
  -- ({\rI*sin(\phB)*cos(\thB)},{\rI*sin(\phB)*sin(\thB)},{\rI*cos(\phB)})
  -- ({\rO*sin(\phB)*cos(\thB)},{\rO*sin(\phB)*sin(\thB)},{\rO*cos(\phB)})
  -- ({\rO*sin(\phA)*cos(\thB)},{\rO*sin(\phA)*sin(\thB)},{\rO*cos(\phA)}) -- cycle;

% Draw wedge edges
\draw[orange!90!black,very thick]
  ({\rI*sin(\phA)*cos(\thA)},{\rI*sin(\phA)*sin(\thA)},{\rI*cos(\phA)})
  -- ({\rI*sin(\phB)*cos(\thA)},{\rI*sin(\phB)*sin(\thA)},{\rI*cos(\phB)})
  -- ({\rO*sin(\phB)*cos(\thA)},{\rO*sin(\phB)*sin(\thA)},{\rO*cos(\phB)})
  -- ({\rO*sin(\phA)*cos(\thA)},{\rO*sin(\phA)*sin(\thA)},{\rO*cos(\phA)}) -- cycle;
\draw[orange!90!black,very thick]
  ({\rI*sin(\phA)*cos(\thB)},{\rI*sin(\phA)*sin(\thB)},{\rI*cos(\phA)})
  -- ({\rI*sin(\phB)*cos(\thB)},{\rI*sin(\phB)*sin(\thB)},{\rI*cos(\phB)})
  -- ({\rO*sin(\phB)*cos(\thB)},{\rO*sin(\phB)*sin(\thB)},{\rO*cos(\phB)})
  -- ({\rO*sin(\phA)*cos(\thB)},{\rO*sin(\phA)*sin(\thB)},{\rO*cos(\phA)}) -- cycle;
\draw[orange!90!black,very thick]
  ({\rI*sin(\phA)*cos(\thA)},{\rI*sin(\phA)*sin(\thA)},{\rI*cos(\phA)})
  -- ({\rI*sin(\phA)*cos(\thB)},{\rI*sin(\phA)*sin(\thB)},{\rI*cos(\phA)});
\draw[orange!90!black,very thick]
  ({\rI*sin(\phB)*cos(\thA)},{\rI*sin(\phB)*sin(\thA)},{\rI*cos(\phB)})
  -- ({\rI*sin(\phB)*cos(\thB)},{\rI*sin(\phB)*sin(\thB)},{\rI*cos(\phB)});
\draw[orange!90!black,very thick]
  ({\rO*sin(\phA)*cos(\thA)},{\rO*sin(\phA)*sin(\thA)},{\rO*cos(\phA)})
  -- ({\rO*sin(\phA)*cos(\thB)},{\rO*sin(\phA)*sin(\thB)},{\rO*cos(\phA)});
\draw[orange!90!black,very thick]
  ({\rO*sin(\phB)*cos(\thA)},{\rO*sin(\phB)*sin(\thA)},{\rO*cos(\phB)})
  -- ({\rO*sin(\phB)*cos(\thB)},{\rO*sin(\phB)*sin(\thB)},{\rO*cos(\phB)});

% Labels: edge dimensions on front face
\draw[red!70!black,<->,thick]
  ({\rI*sin(\phA)*cos(\thA)},{\rI*sin(\phA)*sin(\thA)},{\rI*cos(\phA)})
  -- ({\rO*sin(\phA)*cos(\thA)},{\rO*sin(\phA)*sin(\thA)},{\rO*cos(\phA)})
  node[midway,above,font=\scriptsize,red!70!black]{$d\rho$};

\draw[green!60!black,<->,thick]
  ({\rO*sin(\phA)*cos(\thA)},{\rO*sin(\phA)*sin(\thA)},{\rO*cos(\phA)})
  -- ({\rO*sin(\phB)*cos(\thA)},{\rO*sin(\phB)*sin(\thA)},{\rO*cos(\phB)})
  node[midway,right,font=\scriptsize,green!50!black]{$\rho\,d\phi$};

% theta arc label
\node[font=\scriptsize,blue!70!black,align=center] at
  ({(\rO+0.25)*sin(55)*cos(\thA)},{(\rO+0.25)*sin(55)*sin(\thA)},{(\rO+0.25)*cos(55)})
  {$\rho\sin\phi$\\$d\theta$};

% dV label
\node[font=\small,orange!80!black,align=center] at (-2.5,-2.5,0)
  {$dV = \rho^2\sin\phi\,d\rho\,d\phi\,d\theta$};
\draw[orange!70!black,->,thin]
  (1.5,-1.0,0.7) -- ({\rO*sin(55)*cos(32)},{\rO*sin(55)*sin(32)},{\rO*cos(55)});

% rho vector to P
\draw[violet,thick,->] (0,0,0) -- ({\rO*sin(\phA)*cos(\thA)},{\rO*sin(\phA)*sin(\thA)},{\rO*cos(\phA)})
  node[above right,font=\scriptsize,violet]{$\rho$};


\end{tikzpicture}
\end{center}


Multi-varialbe integrations, such as $\iiint f\cdot dV$ or $\iint f\cdot dA$ require a way to measure the size of the tiny input regions of the domain (such as $dA$ and $dV$).

Whereas last module we analyzed the geometries of polar and spherical coordiantes to determine the size measures $dA=r\,dr\,d\theta$ and $dV = \rho^2\sin\phi\,d\rho\,d\phi\,d\theta$, in this module we will discover a more general process for determining the domain measures of size: The \textbf{Jacobian} matrix and its \emph{determinant}.

\section*{Matrices and Linear Transformations}

The key ideas that allow us to efficiently determine the differential measures like $dA$ and $dV$ are that:

\begin{enumerate}
  \item Derivatives skew space linearly
  \item Linear transformations can be represented by matrices
\end{enumerate}

In single-variable calculus, we created linear versions of functions $f$ by the differential $df = f'(x)dx$. 

Now, we're going to do the same thing to coordinate transformations $T$ using what is called the \textbf{Jacobian matrix} $J$ of $T$. The Jacobian matrix is the matrix of all partial derivatives of the transformation $T$.

If $T$ maps $(u,v)$ to $(x,y)$, then the Jacobian matrix is:

$$J = \begin{bmatrix}\frac{\partial x}{\partial u} & \frac{\partial x}{\partial v} \\[6pt] \frac{\partial y}{\partial u} & \frac{\partial y}{\partial v}\end{bmatrix}$$

We can use $J$ to find the measures such as $dA$ and $dV$ in a similar differential relationship:

$$dA = |\det(J)|\,du\,dv$$

Here, $\det(J)$ refers to the \emph{determinant} of the Jacobian matrix.

But why all the fuss about a matrix $J$ of derivatives?

And why does the determinant of $J$ give us the measure of area or volume? 

We need to dig in to the idea of linear transformations and matrix multiplication to understand this.

The following video by 3Blue1Brown gives a great breakdown of how to think about matrices as linear transformations and how matrix multiplication efficiently captures how matrices transform all of space:

\youtube{kYB8IZa5AuE?si=bEjVUFTC9psmBwH6}



\begin{problem}
  Let's practice using matrix multiplication to transform vectors.
  \begin{enumerate}
    \item The matrix $A = \begin{bmatrix}2 & 0 \\ 0 & 1\end{bmatrix}$ sends the input vector $\vec{v} = \begin{bmatrix}1 \\ 1\end{bmatrix}$ to the output vector $\vec{w}$ from the multiplication
    
    $$\vec{w} = A\vec{v}= \begin{bmatrix}2 & 0 \\ 0 & 1\end{bmatrix}\begin{bmatrix}1 \\ 1\end{bmatrix}$$

    $$=\answer{1}\begin{bmatrix}
      \answer{2} \\ \answer{1}
    \end{bmatrix} + \answer{1}\begin{bmatrix}
      \answer{0} \\ \answer{1}
    \end{bmatrix} = \begin{bmatrix}
      \answer{2} \\ \answer{1}
    \end{bmatrix}$$

  \item The matrix $B = \begin{bmatrix}1 & 1 \\ 0 & 1\end{bmatrix}$ sends the input vector $\vec{v} = \begin{bmatrix}1 \\ 1\end{bmatrix}$ to the output vector $\vec{w}$ from the multiplication

    $$\vec{w} = B\vec{v}= \begin{bmatrix}1 & 1 \\ 0 & 1\end{bmatrix}\begin{bmatrix}1 \\ 1\end{bmatrix}$$

    $$=\answer{1}\begin{bmatrix}
      \answer{1} \\ \answer{0}
    \end{bmatrix} + \answer{1}\begin{bmatrix}
      \answer{1} \\ \answer{1}
    \end{bmatrix} = \begin{bmatrix}
      \answer{2} \\ \answer{1}
    \end{bmatrix}$$
  \end{enumerate}
\end{problem}

%----------------------------------------------------------------------
\section*{The Polar Transformation Differential}
%----------------------------------------------------------------------

\subsection*{Parameter Space vs.\ Coordinate Space}

The polar transformation is:

\[
  x = r\cos\theta, \qquad y = r\sin\theta.
\]

Because derivatives give us accurate linear approximations, the Jacobian matrix $J$ gives us a linear approximation of the transformation $T$ near any point $(r,\theta)$ in the parameter space.

\begin{problem}
Let's calcualte the Jacobian matrix $J$ of the polar transformation $T$:

\[
  J = \begin{bmatrix}
    \frac{\partial x}{\partial r} & \frac{\partial x}{\partial \theta} \\[6pt]
    \frac{\partial y}{\partial r} & \frac{\partial y}{\partial \theta}
  \end{bmatrix}
  = \begin{bmatrix}
    \answer{\cos(\theta)} & \answer{-r\cdot\sin(\theta)} \\[6pt]
    \answer{\sin(\theta)} & \answer{r\cdot \cos(\theta)}
  \end{bmatrix}.
\]
\end{problem}

% --- Linearized Polar Transformation Figure ---
% Shows parameter-space rectangle → Jacobian parallelogram in xy-space
% Evaluated at (r_0, theta_0) = (2, 30°).
% J columns: e_r = (cos θ_0, sin θ_0)Δr  and  e_θ = (−r_0 sinθ_0, r_0 cosθ_0)Δθ
\begin{center}
\begin{tikzpicture}[scale=1.2]

% Display scale factor applied to J column vectors
\pgfmathsetmacro{\thz}{30}   % theta_0 (degrees)
\pgfmathsetmacro{\rz}{2}     % r_0
\pgfmathsetmacro{\sc}{1.35}  % visual length scale

% Column 1 of J (image of Δr * ê_r): (cos θ_0, sin θ_0)
\pgfmathsetmacro{\erx}{cos(\thz)*\sc}
\pgfmathsetmacro{\ery}{sin(\thz)*\sc}
% Column 2 of J (image of Δθ * ê_θ): (−r_0 sinθ_0, r_0 cosθ_0)  scaled by sc/r_0 for display
\pgfmathsetmacro{\etx}{-\rz*sin(\thz)*\sc/\rz}
\pgfmathsetmacro{\ety}{\rz*cos(\thz)*\sc/\rz}

%=== LEFT: parameter (r, theta) space ===
\begin{scope}[xshift=-0.5cm]
  \draw[->](-0.2,0)--(3.2,0) node[right]{$r$};
  \draw[->](0,-0.2)--(0,2.8) node[above]{$\theta$};
  \draw[gray!30,thin] (0,0) grid[step=0.6] (3.0,2.6);

  % Highlighted cell at roughly (r_0, θ_0) in the grid
  \def\px{1.8}  \def\py{1.2}  \def\dg{0.6}
  \fill[blue!20] (\px,\py) rectangle (\px+\dg,\py+\dg);
  \draw[blue!60!black,thick] (\px,\py) rectangle (\px+\dg,\py+\dg);

  % Basis step vectors in parameter space
  \draw[->,orange!80!black,very thick] (\px,\py) -- (\px+\dg,\py)
        node[midway,below,font=\small]{$\begin{bmatrix} dr \\ 0 \end{bmatrix}$};
  \draw[->,violet!80!black,very thick] (\px,\py) -- (\px,\py+\dg)
        node[midway,left,font=\small]{$\begin{bmatrix} 0 \\ d\theta \end{bmatrix}$};

  % Base point dot and label
  \fill[black] (\px,\py) circle (1.5pt);
  \node[font=\scriptsize,below left] at (\px,\py) {$(r_0,\theta_0)$};

  \node[font=\small,align=center,below] at (1.5,-0.5)
        {\textbf{Parameter space} $(r,\theta)$\\with small cell of area $dr\,d\theta$};
\end{scope}

%=== ARROW with J label === use the actual matrix equation for J as the label
\begin{scope}[xshift=3.5cm, yshift=1.0cm]
  \draw[->,very thick,gray!60](0,0)--(1.3,0) node[midway,above,font=\small]{$J$};
  \node[gray!60,font=\scriptsize,align=center] at (0.65,-0.35)
        {Jacobian\\matrix};
\end{scope}

%=== RIGHT: xy space — parallelogram (linearized image) ===
\begin{scope}[xshift=5.4cm, yshift=0.1cm]
  \draw[->](-0.2,0)--(3.2,0) node[right]{$x$};
  \draw[->](0,-0.3)--(0,2.8) node[above]{$y$};

  % Base point
  \coordinate (P) at (2.5, 0.55);

  % Parallelogram vertices
  \coordinate (Q) at ($(P)+(\erx,\ery)$);        % P + e_r
  \coordinate (R) at ($(P)+(\etx,\ety)$);         % P + e_theta
  \coordinate (S) at ($(P)+(\erx+\etx,\ery+\ety)$); % opposite corner

  % Fill and outline
  \fill[blue!20] (P)--(Q)--(S)--(R)--cycle;
  \draw[blue!60!black,thick] (P)--(Q)--(S)--(R)--cycle;

  % Column vector 1: e_r
  \draw[->,orange!80!black,very thick] (P)--(Q)
        node[midway,below right,font=\footnotesize,align=left]
        {$dr\begin{bmatrix} \cos(\theta_0) \\ \sin(\theta_0) \end{bmatrix}$};
  % Column vector 2: e_theta
  \draw[->,violet!80!black,very thick] (P)--(R)
        node[midway,left,font=\footnotesize,align=right]
        {$d\theta\begin{bmatrix} -r_0\sin(\theta_0) \\ r_0\cos(\theta_0) \end{bmatrix}$};

  % Base point dot
  \fill[black] (P) circle (1.5pt);
  \node[font=\scriptsize,below left] at (P) {$T(r_0,\theta_0)$};

  % Small right-angle marker (e_r ⊥ e_theta for polar)
  \pgfmathsetmacro{\normEr}{sqrt(\erx*\erx+\ery*\ery)}
  \pgfmathsetmacro{\ux}{\erx/\normEr*0.18}
  \pgfmathsetmacro{\uy}{\ery/\normEr*0.18}
  \pgfmathsetmacro{\normEt}{sqrt(\etx*\etx+\ety*\ety)}
  \pgfmathsetmacro{\vx}{\etx/\normEt*0.18}
  \pgfmathsetmacro{\vy}{\ety/\normEt*0.18}
  \draw[gray!60,thin]
    ($(P)+(\ux,\uy)$) -- ($(P)+(\ux+\vx,\uy+\vy)$) -- ($(P)+(\vx,\vy)$);

  \node[font=\small,align=center,below] at (1.5,-0.6)
        {\textbf{Linearized image in $(x,y)$}\\perpendicular sides of lengths $d r$ and $r_0\,d\theta$};
\end{scope}

\end{tikzpicture}
\end{center}

The small \textbf{blue rectangle} in parameter space has:
\begin{itemize}
  \item width $= dr$ (a change in radius)
  \item height $= d\theta$ (a change in angle)
\end{itemize}

After the transformation, the resulting region is a parallelogram in coordinate space. 

What is the area of the parallelogram in coordinate space?

For this, we need the \textbf{determinant} of the Jacobian matrix $J$.


\subsection*{Determinants}

Determinants measure how much a linear transformation stretches or compresses space. For our purposes, this directly measures how much the original rectangle $dr\,d\theta$ in parameter space is stretched or compressed when transformed into coordinate space.

\begin{definition}
The \emph{determinant} of a matrix 

$$M=\begin{bmatrix} a & c \\ b & d \end{bmatrix}$$

is given by:
$$\text{det}(M) = ad - bc$$

The determinant measures how much area is scaled by the linear transformation represented by $M$.

\end{definition}

This means that we can directly measure the $dA$ under the transformation $T$ by taking the absolute value of the determinant of the Jacobian matrix $J$ of $T$.

\section*{Re-deriving the Polar Area Element}

Back to our polar example, if $T$ is the transformation $x=r\cos\theta$ and $y=r\sin\theta$, then the Jacobian matrix is

$$J = \begin{bmatrix}
    \cos(\theta) & -r\sin(\theta) \\[6pt]
    \sin(\theta) & r\cos(\theta)
  \end{bmatrix}$$

The measure of the differential area $dA$ in coordinate space is given by:

$$dA=|\det(J)|\,dr\,d\theta$$

from our definition, 

$$\det(J) = \cos(\theta)\cdot r\cos(\theta) - (-r\sin(\theta))\cdot \sin(\theta)$$

using the Pythagorean Identity we get

$$r\cos^2(\theta) + r\sin^2(\theta) = r.$$

This is the same result we found originally for the polar area element $dA = r\,dr\,d\theta$!

\section*{General Transformations}

Given \emph{any} transformation $T:(u,v)\mapsto(x,y)$:
\[
  x = x(u,v),\qquad y = y(u,v),
\]
how does a small $du\,dv$ rectangle transform?

We again use the Jacobian matrix:

$$J=\begin{bmatrix}
    \frac{\partial x}{\partial u} & \frac{\partial x}{\partial v} \\[6pt]
    \frac{\partial y}{\partial u} & \frac{\partial y}{\partial v}
  \end{bmatrix}$$

We then find the transformed area using the determinant of $J$:

$$dA = \left|\det(J)\right|\,du\,dv = \left|\frac{\partial x}{\partial u}\frac{\partial y}{\partial v} - \frac{\partial x}{\partial v}\frac{\partial y}{\partial u}\right|\,du\,dv.$$

The image is approximately a \emph{parallelogram} spanned by $\vec{e}_u$ and $\vec{e}_v$.

\begin{center}
\begin{tikzpicture}[scale=1.05]

%--- LEFT: small rectangle in uv-space ---
\begin{scope}[xshift=0cm]
  \draw[->](-0.2,0)--(3.3,0) node[right]{$u$};
  \draw[->](0,-0.2)--(0,2.8) node[above]{$v$};

  \fill[blue!20] (1.2,0.9) rectangle (2.5,2.0);
  \draw[blue!60!black, thick] (1.2,0.9) rectangle (2.5,2.0);

  \draw[->, orange!80!black, very thick] (1.2,0.9) -- (2.5,0.9)
        node[midway, below, font=\small]{$\begin{bmatrix} \Delta u \\ 0 \end{bmatrix}$};
  \draw[->, violet!80!black, very thick] (1.2,0.9) -- (1.2,2.0)
        node[midway, left, font=\small]{$\begin{bmatrix} 0 \\ \Delta v \end{bmatrix}$};

  \fill[black] (1.2,0.9) circle (1.5pt);
  \node[font=\scriptsize,below left] at (1.2,0.9) {$(u_0,v_0)$};
  \node[blue!70!black, font=\small] at (1.85, 1.45) {$\Delta A = \Delta u\,\Delta v$};
  \node[below] at (1.5, -0.35) {\textbf{Parameter space}};
\end{scope}

%--- ARROW ---
\begin{scope}[xshift=3.7cm, yshift=0.9cm]
  \draw[->,very thick,gray!60](0,0)--(1.1,0) node[midway,above,font=\small]{$J$};
  \node[gray!60, font=\scriptsize, align=center] at (0.55,-0.3) {Jacobian\\matrix};
\end{scope}

%--- RIGHT: parallelogram in xy-space ---
\begin{scope}[xshift=5.2cm]
  \draw[->](-0.2,0)--(3.3,0) node[right]{$x$};
  \draw[->](0,-0.2)--(0,2.8) node[above]{$y$};

  \coordinate (A) at (0.7,0.6);
  \coordinate (B) at (2.1,1.0);
  \coordinate (C) at (1.1,1.9);
  \coordinate (D) at (2.5,2.3);

  \fill[blue!20] (A)--(B)--(D)--(C)--cycle;
  \draw[blue!60!black, thick] (A)--(B)--(D)--(C)--cycle;

  \fill[black] (A) circle (1.5pt);
  \node[font=\scriptsize,below left] at (A) {$T(u_0,v_0)$};

  \draw[->, orange!80!black, very thick] (A)--(B)
        node[midway, below right, xshift=2pt, font=\small]
        {$\Delta u\begin{bmatrix} \partial x/\partial u \\ \partial y/\partial u \end{bmatrix}$};
  \draw[->, violet!80!black, very thick] (A)--(C)
        node[midway, left, font=\small]
        {$\Delta v\begin{bmatrix} \partial x/\partial v \\ \partial y/\partial v \end{bmatrix}$};

  \node[blue!70!black, font=\small] at (1.6, 1.45)
        {$\Delta A \approx |J|\,\Delta u\,\Delta v$};

  \node[below] at (1.6, -0.35) {\textbf{Coordinate space}};
\end{scope}

\end{tikzpicture}
\end{center}


\begin{problem}
So the Jacobian determinant is:
\begin{multipleChoice}
  \choice{An arbitrary formula}
  \choice{The length of the image vectors}
  \choice[correct]{The area of the parallelogram formed by the images of the unit basis vectors}
  \choice{The sum of the partial derivatives}
\end{multipleChoice}
\end{problem}

\begin{problem}
Let's verify that the polar Jacobian comes from this formula.

For $x = r\cos\theta$, $y = r\sin\theta$:

$$\frac{\partial x}{\partial r} = \answer{\cos\theta}, \quad \frac{\partial x}{\partial \theta} = \answer{-r\sin\theta}$$

$$\frac{\partial y}{\partial r} = \answer{\sin\theta}, \quad \frac{\partial y}{\partial \theta} = \answer{r\cos\theta}$$

The Jacobian determinant is:
$$J = (\cos\theta)(r\cos\theta) - (-r\sin\theta)(\sin\theta) = r\cos^2\theta + r\sin^2\theta = \answer{r}$$

This matches the geometric derivation. $\checkmark$

The two column vectors of $\mathbf{J}$ are $\vec{e}_r = (\cos\theta,\sin\theta)$ and $\vec{e}_\theta = (-r\sin\theta, r\cos\theta)$.

Their lengths:
\begin{itemize}
  \item $|\vec{e}_r| = \answer{1}$ (unit radial vector)
  \item $|\vec{e}_\theta| = \answer{r}$ (tangent to a circle of radius $r$)
\end{itemize}

These vectors are perpendicular (polar coordinates are \emph{orthogonal}), so the parallelogram is a rectangle of area $1 \cdot r = r$ --- exactly the Jacobian! $\checkmark$

\begin{feedback}
For orthogonal coordinate systems (polar, cylindrical, spherical), the Jacobian is simply the product of the scale factors along each coordinate direction.  For polar: scale factors are $1$ (radial) and $r$ (angular), giving $J = 1 \cdot r = r$.
\end{feedback}
\end{problem}


For a transformation $T:(u,v)\mapsto(x,y)$ defined by $x = x(u,v)$, $y = y(u,v)$, the \textbf{Jacobian matrix} is:
$$\mathbf{J} = \begin{bmatrix}
\dfrac{\partial x}{\partial u} & \dfrac{\partial x}{\partial v}\\[10pt]
\dfrac{\partial y}{\partial u} & \dfrac{\partial y}{\partial v}
\end{bmatrix}$$

The \textbf{Jacobian determinant} is:
$$J = \det(\mathbf{J}) = \dfrac{\partial x}{\partial u}\dfrac{\partial y}{\partial v} - \dfrac{\partial x}{\partial v}\dfrac{\partial y}{\partial u}$$

The absolute value $|J|$ is the \textbf{local area scaling factor}: the transformation maps a tiny area element $du\,dv$ to an area element $|J|\,du\,dv$ in the $(x,y)$-plane.
%----------------------------------------------------------------------
\section*{More Transformations}
%----------------------------------------------------------------------


\begin{problem}
Now consider the \emph{complex squaring map}: $x = u^2 - v^2$, $y = 2uv$.

Compute the Jacobian:
$$\frac{\partial x}{\partial u} = \answer{2u}, \quad \frac{\partial x}{\partial v} = \answer{-2v}$$
$$\frac{\partial y}{\partial u} = \answer{2v}, \quad \frac{\partial y}{\partial v} = \answer{2u}$$

$$J = (2u)(2u) - (-2v)(2v) = \answer{4u^2 + 4v^2} = 4(u^2+v^2)$$

So $|J| = 4(u^2+v^2)$.  This tells us:
\begin{selectAll}
  \choice[correct]{Area scaling grows quadratically with distance from the origin}
  \choice{Near the origin, area is stretched the most}
  \choice[correct]{Near the origin, the transformation almost collapses area to zero}
  \choice[correct]{The transformation is degenerate ($J=0$) only at the origin}
\end{selectAll}

\end{problem}

\textbf{The complex squaring map transforms a uniform grid into families of intersecting parabolas:}

\begin{center}
\begin{tikzpicture}[scale=1.1]

%=== LEFT: (u,v) parameter grid ===
\begin{scope}[xshift=0cm]
  \draw[->](-1.5,0)--(1.5,0) node[right,font=\small]{$u$};
  \draw[->](0,-1.5)--(0,1.5) node[above,font=\small]{$v$};
  % Horizontal grid lines v=const (orange family)
  \draw[orange!90!black, semithick] (-1.2,-1.0)--(1.2,-1.0);
  \draw[orange!70!black, semithick] (-1.2,-0.5)--(1.2,-0.5);
  \draw[orange!40!black, semithick] (-1.2, 0.0)--(1.2, 0.0);
  \draw[orange!70!black, semithick] (-1.2, 0.5)--(1.2, 0.5);
  \draw[orange!90!black, semithick] (-1.2, 1.0)--(1.2, 1.0);
  % Vertical grid lines u=const (violet family)
  \draw[violet!90!black, semithick] (-1.0,-1.2)--(-1.0, 1.2);
  \draw[violet!70!black, semithick] (-0.5,-1.2)--(-0.5, 1.2);
  \draw[violet!40!black, semithick] ( 0.0,-1.2)--( 0.0, 1.2);
  \draw[violet!70!black, semithick] ( 0.5,-1.2)--( 0.5, 1.2);
  \draw[violet!90!black, semithick] ( 1.0,-1.2)--( 1.0, 1.2);
  % Highlighted cell [0.7,0.9]x[0.4,0.6]
  \fill[green!35,opacity=0.75] (0.7,0.4) rectangle (0.9,0.6);
  \draw[green!60!black,thick] (0.7,0.4) rectangle (0.9,0.6);
  \node[font=\small,align=center,below] at (0,-1.75)
        {\textbf{Parameter space} $(u,v)$\\equal rectangular cells};
\end{scope}

%=== ARROW ===
\begin{scope}[xshift=2.05cm,yshift=0.05cm]
  \draw[->,very thick,gray!60](0,0)--(1.0,0) node[midway,above,font=\small]{$T$};
  \node[gray!60,font=\scriptsize,align=center] at (0.5,-0.3)
        {$x{=}u^2{-}v^2$\\[-1pt]$y{=}2uv$};
\end{scope}

%=== RIGHT: image grid ===
\begin{scope}[xshift=3.2cm]
  \draw[->](-1.5,0)--(1.5,0) node[right,font=\small]{$x$};
  \draw[->](0,-1.5)--(0,1.5) node[above,font=\small]{$y$};
  \begin{scope}
    \clip (-1.45,-1.45) rectangle (1.45,1.45);
    % Images of v=-1: x=t^2-1, y=-2t
    \draw[orange!90!black,semithick,variable=\t,domain=-1.2:1.2,samples=45]
      plot ({\t*\t-1.0},{-2.0*\t});
    % Images of v=-0.5: x=t^2-0.25, y=-t
    \draw[orange!70!black,semithick,variable=\t,domain=-1.2:1.2,samples=45]
      plot ({\t*\t-0.25},{-1.0*\t});
    % v=0 maps to nonneg x-axis
    \draw[orange!40!black,semithick] (0,0)--(1.44,0);
    % Images of v=0.5: x=t^2-0.25, y=t
    \draw[orange!70!black,semithick,variable=\t,domain=-1.2:1.2,samples=45]
      plot ({\t*\t-0.25},{1.0*\t});
    % Images of v=1: x=t^2-1, y=2t
    \draw[orange!90!black,semithick,variable=\t,domain=-1.2:1.2,samples=45]
      plot ({\t*\t-1.0},{2.0*\t});
    % Images of u=-1: x=1-t^2, y=-2t
    \draw[violet!90!black,semithick,variable=\t,domain=-1.2:1.2,samples=45]
      plot ({1.0-\t*\t},{-2.0*\t});
    % Images of u=-0.5: x=0.25-t^2, y=-t
    \draw[violet!70!black,semithick,variable=\t,domain=-1.2:1.2,samples=45]
      plot ({0.25-\t*\t},{-1.0*\t});
    % u=0 maps to nonpos x-axis
    \draw[violet!40!black,semithick] (-1.44,0)--(0,0);
    % Images of u=0.5: x=0.25-t^2, y=t
    \draw[violet!70!black,semithick,variable=\t,domain=-1.2:1.2,samples=45]
      plot ({0.25-\t*\t},{1.0*\t});
    % Images of u=1: x=1-t^2, y=2t
    \draw[violet!90!black,semithick,variable=\t,domain=-1.2:1.2,samples=45]
      plot ({1.0-\t*\t},{2.0*\t});
    % Image of highlighted cell [0.7,0.9]x[0.4,0.6]:
    % T(0.7,0.4)=(0.33,0.56), T(0.9,0.4)=(0.65,0.72), T(0.9,0.6)=(0.45,1.08), T(0.7,0.6)=(0.13,0.84)
    \fill[green!25,opacity=0.7]
      (0.33,0.56)--(0.65,0.72)--(0.45,1.08)--(0.13,0.84)--cycle;
    \draw[green!60!black,thick]
      (0.33,0.56)--(0.65,0.72)--(0.45,1.08)--(0.13,0.84)--cycle;
  \end{scope}
  \fill[black] (0,0) circle (1.5pt);
  \node[font=\tiny,below right] at (0,0) {$0$};
  \node[font=\small,align=center,below] at (0,-1.75)
        {\textbf{Image under $T$}\\straight lines $\to$ parabolas;
         \\cells grow with distance from origin};
\end{scope}

\end{tikzpicture}
\end{center}

\begin{itemize}
  \item \textbf{Conformal:} the orange and violet families of parabolas intersect at right angles everywhere --- the squaring map preserves angles.
  \item \textbf{Area scaling:} $|J| = 4(u^2+v^2)$ grows quadratically, so cells near the origin get squished while cells farther out get stretched.
  \item \textbf{Degenerate at origin:} $J(0,0) = 0$, the map collapses area there (the two families both send the axes to the $x$-axis).
\end{itemize}

\textbf{Local linear (Jacobian) approximation at $(u_0,v_0)=(1,\tfrac{1}{2})$:}

\begin{center}
\begin{tikzpicture}[scale=1.2]

% J at (1, 0.5): [[2u0,-2v0],[2v0,2u0]] = [[2,-1],[1,2]]
% Display scale factor 0.5 applied to column vectors
\pgfmathsetmacro{\euxE}{1.0}   % (2)*(0.5)
\pgfmathsetmacro{\euyE}{0.5}   % (1)*(0.5)
\pgfmathsetmacro{\evxE}{-0.5}  % (-1)*(0.5)
\pgfmathsetmacro{\evyE}{1.0}   % (2)*(0.5)

%=== LEFT: parameter (u,v) space ===
\begin{scope}[xshift=-0.5cm]
  \draw[->](-0.2,0)--(3.2,0) node[right]{$u$};
  \draw[->](0,-0.2)--(0,2.8) node[above]{$v$};
  \draw[gray!30,thin] (0,0) grid[step=0.6] (3.0,2.6);

  % Cell at display position (1.2, 0.6) = grid cell for (u0,v0)=(1.0,0.5)
  \def\px{1.2}  \def\py{0.6}  \def\dg{0.6}
  \fill[blue!20] (\px,\py) rectangle (\px+\dg,\py+\dg);
  \draw[blue!60!black,thick] (\px,\py) rectangle (\px+\dg,\py+\dg);

  \draw[->,orange!80!black,very thick] (\px,\py) -- (\px+\dg,\py)
        node[midway,below,font=\small]{$\begin{bmatrix}\Delta u \\ 0\end{bmatrix}$};
  \draw[->,violet!80!black,very thick] (\px,\py) -- (\px,\py+\dg)
        node[midway,left,font=\small]{$\begin{bmatrix}0 \\ \Delta v\end{bmatrix}$};

  \fill[black] (\px,\py) circle (1.5pt);
  \node[font=\scriptsize,below left] at (\px,\py) {$(u_0,v_0)$};

  \node[font=\small,align=center,below] at (1.5,-0.5)
        {\textbf{Parameter space} $(u,v)$\\cell at $(1,\tfrac{1}{2})$};
\end{scope}

%=== ARROW ===
\begin{scope}[xshift=3.5cm, yshift=1.0cm]
  \draw[->,very thick,gray!60](0,0)--(1.3,0) node[midway,above,font=\small]{$J$};
  \node[gray!60,font=\scriptsize,align=center] at (0.65,-0.35)
        {Jacobian\\matrix};
\end{scope}

%=== RIGHT: xy space — linearized parallelogram ===
\begin{scope}[xshift=5.4cm, yshift=0.0cm]
  \draw[->](-0.2,0)--(3.2,0) node[right]{$x$};
  \draw[->](0,-0.3)--(0,2.8) node[above]{$y$};

  \coordinate (PE) at (1.3, 0.8);
  \coordinate (QE) at ($(PE)+(\euxE,\euyE)$);
  \coordinate (RE) at ($(PE)+(\evxE,\evyE)$);
  \coordinate (SE) at ($(PE)+(\euxE+\evxE,\euyE+\evyE)$);

  \fill[blue!20] (PE)--(QE)--(SE)--(RE)--cycle;
  \draw[blue!60!black,thick] (PE)--(QE)--(SE)--(RE)--cycle;

  \draw[->,orange!80!black,very thick] (PE)--(QE)
        node[midway,below right,font=\footnotesize,align=left]
        {$\Delta u\begin{bmatrix}2\\1\end{bmatrix}$};
  \draw[->,violet!80!black,very thick] (PE)--(RE)
        node[midway,left,font=\footnotesize,align=right]
        {$\Delta v\begin{bmatrix}-1\\2\end{bmatrix}$};

  \fill[black] (PE) circle (1.5pt);
  \node[font=\scriptsize,below left] at (PE) {$T(u_0,v_0)$};

  % Right-angle marker (conformal: e_u perp e_v)
  \pgfmathsetmacro{\nEuE}{sqrt(\euxE*\euxE+\euyE*\euyE)}
  \pgfmathsetmacro{\uuEx}{\euxE/\nEuE*0.18}
  \pgfmathsetmacro{\uuEy}{\euyE/\nEuE*0.18}
  \pgfmathsetmacro{\nEvE}{sqrt(\evxE*\evxE+\evyE*\evyE)}
  \pgfmathsetmacro{\uvEx}{\evxE/\nEvE*0.18}
  \pgfmathsetmacro{\uvEy}{\evyE/\nEvE*0.18}
  \draw[gray!60,thin]
    ($(PE)+(\uuEx,\uuEy)$) -- ($(PE)+(\uuEx+\uvEx,\uuEy+\uvEy)$) -- ($(PE)+(\uvEx,\uvEy)$);

  \draw[red!60!black,thick,rounded corners] (-0.15,-0.55) rectangle (3.15,-0.1);
  \node[red!60!black,font=\small] at (1.5,-0.32)
        {$|\det J|{=}(2)(2){-}({-}1)(1){=}5$ at $(u_0,v_0){=}(1,\tfrac{1}{2})$};

  \node[font=\small,align=center,below] at (1.5,-0.58)
        {\textbf{Linearized image in $(x,y)$}\\perpendicular sides; $\vec{e}_u\perp\vec{e}_v$ (conformal)};
\end{scope}

\end{tikzpicture}
\end{center}

Let's practice finding the differential areas $dA$ for other transformations.

\begin{problem}
The region $R$ is bounded by:
$$x - y = 0,\quad x - y = 2,\quad x + y = 1,\quad x + y = 3$$

This is a parallelogram whose sides are parallel to the diagonals.  Introduce:
$$u = x - y, \qquad v = x + y$$

Then the new bounds for the region are:
$$\answer{0} \leq u \leq \answer{2}, \qquad \answer{1} \leq v \leq \answer{3}$$

\textbf{Solve for $x$ and $y$:}

Adding: $u + v = 2x$, so $x = \dfrac{u+v}{\answer{2}}$.

Subtracting: $v - u = 2y$, so $y = \dfrac{v-u}{\answer{2}}$.

\textbf{Compute the Jacobian:}
$$\frac{\partial x}{\partial u} = \answer{1/2}, \quad \frac{\partial x}{\partial v} = \answer{1/2}, \quad \frac{\partial y}{\partial u} = \answer{-1/2}, \quad \frac{\partial y}{\partial v} = \answer{1/2}$$

$$J = \frac{1}{2}\cdot\frac{1}{2} - \frac{1}{2}\cdot\left(-\frac{1}{2}\right) = \answer{1/2}$$

\textbf{Area of the parallelogram $R$:}
$$\text{Area} = \int_1^3\int_0^2 \frac{1}{2}\,du\,dv = \frac{1}{2}\cdot\answer{2}\cdot\answer{2} = \answer{2}$$

\begin{feedback}
The transformation $u=x-y$, $v=x+y$ rotates the parallelogram into a nice axis-aligned rectangle.  The Jacobian $\frac{1}{2}$ says the $(u,v)$-rectangle has \emph{twice} the area of the parallelogram --- so we multiply by $\frac{1}{2}$ to correct for that.
\end{feedback}
\end{problem}

% --- Linearized Tilted-Parallelogram Transformation ($u=x-y$, $v=x+y$) ---
% T: (u,v) -> (x,y) with x=(u+v)/2, y=(v-u)/2.  J = 1/2 (constant).
% e_u = (1/2, -1/2),  e_v = (1/2, 1/2)  (scaled by display factor for visibility)
\begin{center}
\begin{tikzpicture}[scale=1.3]

% Column vectors of J (scaled ×2 for visibility)
\def\euxA{1.0}  \def\euyA{-1.0}   % e_u = (1/2,-1/2) × 2
\def\evxA{1.0}  \def\evyA{1.0}    % e_v = (1/2, 1/2) × 2

%=== LEFT: parameter (u,v) space ===
\begin{scope}[xshift=-0.4cm]
  \draw[->](-0.2,0)--(3.2,0) node[right]{$u$};
  \draw[->](0,-0.2)--(0,3.0) node[above]{$v$};
  \draw[gray!30,thin] (0,0) grid[step=0.7] (3.0,2.8);

  \fill[blue!20] (0.7,1.4) rectangle (1.4,2.1);
  \draw[blue!60!black,thick] (0.7,1.4) rectangle (1.4,2.1);

  \draw[->,orange!80!black,very thick] (0.7,1.4) -- (1.4,1.4)
        node[midway,below,font=\small]{$\begin{bmatrix}\Delta u \\ 0\end{bmatrix}$};
  \draw[->,violet!80!black,very thick] (0.7,1.4) -- (0.7,2.1)
        node[midway,left,font=\small]{$\begin{bmatrix}0 \\ \Delta v\end{bmatrix}$};

  \fill[black] (0.7,1.4) circle (1.5pt);
  \node[font=\scriptsize,below left] at (0.7,1.4) {$(u_0,v_0)$};
  \node[font=\small,align=center,below] at (1.5,-0.45)
        {\textbf{Parameter space} $(u,v)$\\orthogonal unit rectangle};
\end{scope}

%=== ARROW ===
\begin{scope}[xshift=3.5cm, yshift=1.2cm]
  \draw[->,very thick,gray!60](0,0)--(1.2,0) node[midway,above,font=\small]{$J$};
  \node[gray!60,font=\scriptsize,align=center] at (0.6,-0.35){$J=\tfrac{1}{2}$\\(constant)};
\end{scope}

%=== RIGHT: xy space — parallelogram ===
\begin{scope}[xshift=5.2cm, yshift=0.4cm]
  \draw[->](-0.2,0)--(3.4,0) node[right]{$x$};
  \draw[->](0,-1.3)--(0,2.4) node[above]{$y$};

  \coordinate (P) at (1.0, 0.0);
  \coordinate (Q) at ($(P)+(\euxA,\euyA)$);              % P + e_u
  \coordinate (R) at ($(P)+(\evxA,\evyA)$);              % P + e_v
  \coordinate (S) at ($(P)+(\euxA+\evxA,\euyA+\evyA)$); % opposite corner

  \fill[blue!20] (P)--(Q)--(S)--(R)--cycle;
  \draw[blue!60!black,thick] (P)--(Q)--(S)--(R)--cycle;

  \draw[->,orange!80!black,very thick] (P)--(Q)
        node[midway,right,font=\footnotesize,align=left]
        {$\Delta u\begin{bmatrix}\tfrac{1}{2}\\-\tfrac{1}{2}\end{bmatrix}$};
  \draw[->,violet!80!black,very thick] (P)--(R)
        node[midway,left,font=\footnotesize,align=right]
        {$\Delta v\begin{bmatrix}\tfrac{1}{2}\\\tfrac{1}{2}\end{bmatrix}$};

  \fill[black] (P) circle (1.5pt);
  \node[font=\scriptsize,below right] at (P) {$T(u_0,v_0)$};

  % Right-angle marker
  \pgfmathsetmacro{\nEu}{sqrt(\euxA*\euxA+\euyA*\euyA)}
  \pgfmathsetmacro{\uux}{\euxA/\nEu*0.2}  \pgfmathsetmacro{\uuy}{\euyA/\nEu*0.2}
  \pgfmathsetmacro{\nEv}{sqrt(\evxA*\evxA+\evyA*\evyA)}
  \pgfmathsetmacro{\uvx}{\evxA/\nEv*0.2}  \pgfmathsetmacro{\uvy}{\evyA/\nEv*0.2}
  \draw[gray!60,thin]
    ($(P)+(\uux,\uuy)$) -- ($(P)+(\uux+\uvx,\uuy+\uvy)$) -- ($(P)+(\uvx,\uvy)$);

  % Area label
  \node[blue!70!black,font=\small] at ($(P)+({(\euxA+\evxA)/2},{(\euyA+\evyA)/2})$)
        {Area $= \tfrac{1}{2}\Delta u\,\Delta v$};

  % Annotation box
  \draw[red!60!black,thick,rounded corners] (-0.2,-1.2) rectangle (3.3,-0.85);
  \node[red!60!black,font=\small] at (1.55,-1.02)
        {$|\det J| = \tfrac{1}{2}\cdot\tfrac{1}{2}+\tfrac{1}{2}\cdot\tfrac{1}{2} = \tfrac{1}{2}$
         \;($\vec{e}_u \perp \vec{e}_v$)};

  \node[font=\small,align=center,below] at (1.55,-1.25)
        {\textbf{Linearized image in $(x,y)$}\\rhombus, area halved relative to $(u,v)$ cell};
\end{scope}

\end{tikzpicture}
\end{center}

\subsection*{Example 2: The Ellipse to a Circle}

\begin{problem}
Integrate over the ellipse $\dfrac{x^2}{9} + \dfrac{y^2}{4} \leq 1$.

\textbf{Step 1: Transform.}  Let $u = \dfrac{x}{3}$, $v = \dfrac{y}{2}$, so $u^2+v^2 \leq 1$ (unit disk).

$$x = \answer{3u}, \qquad y = \answer{2v}$$

\textbf{Step 2: Jacobian.}
$$\mathbf{J} = \begin{bmatrix} \answer{3} & \answer{0} \\ \answer{0} & \answer{2} \end{bmatrix}, \qquad J = \answer{6}$$

\textbf{Step 3: Area of the ellipse.}
$$\text{Area} = \iint_{\text{ellipse}} dx\,dy = \iint_{\text{unit disk}} 6\,du\,dv = 6 \cdot \answer{\pi} = \answer{6\pi}$$

Check: $\pi ab = \pi(3)(2) = 6\pi$ \checkmark

\textbf{Step 4: Integrate $f(x,y) = x^2$ over the ellipse.}

In new coordinates, $f = (3u)^2 = \answer{9u^2}$.

Using polar in the unit disk ($u = r\cos\theta$, $v = r\sin\theta$):
$$\iint_{\text{ellipse}} x^2\,dx\,dy = \int_0^{2\pi}\int_0^1 9r^2\cos^2\theta \cdot 6 \cdot \answer{r}\,dr\,d\theta$$
$$= 54\int_0^{2\pi}\cos^2\theta\,d\theta \cdot \int_0^1 r^3\,dr = 54 \cdot \answer{\pi} \cdot \answer{1/4} = \answer{27\pi/2}$$

\begin{feedback}
Chaining two transformations (ellipse $\to$ circle, then circle $\to$ polar) with Jacobians $6$ and $r$ respectively is very powerful.  The combined Jacobian is $6r$.
\end{feedback}
\end{problem}

% --- Linearized Ellipse-to-Circle Transformation ($x=3u$, $y=2v$) ---
% T: (u,v) -> (x,y).  J = 6 (constant axis-aligned scaling).
% e_u = (3,0)Δu,  e_v = (0,2)Δv
\begin{center}
\begin{tikzpicture}[scale=1.2]

% Display scale: show Δu=Δv=1 → e_u=(3,0), e_v=(0,2) scaled by 0.55
\def\euxB{1.65}  \def\euyB{0}     % e_u = (3,0) × 0.55
\def\evxB{0}     \def\evyB{1.1}   % e_v = (0,2) × 0.55

%=== LEFT: parameter (u,v) space — unit disk boundary sketched ===
\begin{scope}[xshift=-0.6cm]
  \draw[->](-1.4,0)--(1.6,0) node[right]{$u$};
  \draw[->](0,-1.4)--(0,1.6) node[above]{$v$};
  % Unit circle (parameter region)
  \draw[blue!50!black,thick,dashed] (0,0) circle [radius=1.0];
  \fill[blue!10,opacity=0.5] (0,0) circle [radius=1.0];
  % Highlighted small square cell
  \fill[blue!30,opacity=0.7] (0.35,0.35) rectangle (0.85,0.85);
  \draw[blue!60!black,thick] (0.35,0.35) rectangle (0.85,0.85);
  % Basis steps
  \draw[->,orange!80!black,very thick] (0.35,0.35) -- (0.85,0.35)
        node[midway,below,font=\small]{$\begin{bmatrix}\Delta u \\ 0\end{bmatrix}$};
  \draw[->,violet!80!black,very thick] (0.35,0.35) -- (0.35,0.85)
        node[midway,left,font=\small]{$\begin{bmatrix}0 \\ \Delta v\end{bmatrix}$};
  \fill[black] (0.35,0.35) circle (1.5pt);
  \node[font=\scriptsize,align=center] at (0,-1.7)
        {\textbf{Parameter space} $(u,v)$\\unit disk $u^2+v^2 \leq 1$};
\end{scope}

%=== ARROW ===
\begin{scope}[xshift=2.0cm, yshift=0.1cm]
  \draw[->,very thick,gray!60](0,0)--(1.2,0) node[midway,above,font=\small]{$J$};
  \node[gray!60,font=\scriptsize,align=center] at (0.6,-0.35){$J=6$\\(constant)};
\end{scope}

%=== RIGHT: xy space — stretched ellipse + parallelogram ===
\begin{scope}[xshift=4.0cm, yshift=0cm]
  \draw[->](-2.0,0)--(2.4,0) node[right]{$x$};
  \draw[->](0,-1.4)--(0,1.6) node[above]{$y$};
  % Ellipse (image of unit disk)
  \draw[blue!50!black,thick,dashed] (0,0) ellipse [x radius=1.65, y radius=1.1];
  \fill[blue!10,opacity=0.4] (0,0) ellipse [x radius=1.65, y radius=1.1];

  % Parallelogram (image of highlighted cell — now an axis-aligned rectangle)
  \coordinate (P2) at (0.58, 0.39);
  \coordinate (Q2) at ($(P2)+(\euxB,\euyB)$);
  \coordinate (R2) at ($(P2)+(\evxB,\evyB)$);
  \coordinate (S2) at ($(P2)+(\euxB+\evxB,\euyB+\evyB)$);

  \fill[blue!30,opacity=0.7] (P2)--(Q2)--(S2)--(R2)--cycle;
  \draw[blue!60!black,thick] (P2)--(Q2)--(S2)--(R2)--cycle;

  \draw[->,orange!80!black,very thick] (P2)--(Q2)
        node[midway,below,font=\footnotesize]
        {$\Delta u\begin{bmatrix}3\\0\end{bmatrix}$};
  \draw[->,violet!80!black,very thick] (P2)--(R2)
        node[midway,left,font=\footnotesize]
        {$\Delta v\begin{bmatrix}0\\2\end{bmatrix}$};

  % Right-angle box
  \draw[gray!60,thin]
    ($(P2)+(0.18,0)$) -- ($(P2)+(0.18,0.18)$) -- ($(P2)+(0,0.18)$);

  \fill[black] (P2) circle (1.5pt);

  % Area label
  \node[blue!70!black,font=\small] at ($(P2)+({(\euxB+\evxB)/2},{(\euyB+\evyB)/2})$)
        {$6\,\Delta u\,\Delta v$};

  % Annotation box
  \draw[red!60!black,thick,rounded corners] (-1.9,-1.65) rectangle (2.3,-1.3);
  \node[red!60!black,font=\small] at (0.2,-1.47)
        {$|\det J| = 3\cdot2 - 0\cdot0 = 6$\;(axis-aligned stretch)};

  \node[font=\small,align=center,below] at (0.2,-1.68)
        {\textbf{Coordinate space} $(x,y)$\\ellipse; rectangle cells stretched by factor 6};
\end{scope}

\end{tikzpicture}
\end{center}

\subsection*{Example 3: Using the Inverse Jacobian}

\begin{problem}
Integrate over the region bounded by $xy = 1$, $xy = 3$, $y = x$, $y = 3x$.

Let $u = xy$ and $v = y/x$.

\textbf{Step 1: New bounds.}
$$\answer{1} \leq u \leq \answer{3}, \qquad \answer{1} \leq v \leq \answer{3}$$

\textbf{Step 2: Solve for $x$ and $y$.}

$uv = (xy)(y/x) = y^2$, so $y = \sqrt{uv}$.  Then $x = u/y = \sqrt{u/v}$.

\textbf{Step 3: Compute the inverse Jacobian} $\partial(u,v)/\partial(x,y)$ (easier here).

With $u = xy$ and $v = y/x$:
$$\frac{\partial u}{\partial x} = \answer{y}, \quad \frac{\partial u}{\partial y} = \answer{x}, \quad \frac{\partial v}{\partial x} = \answer{-y/x^2}, \quad \frac{\partial v}{\partial y} = \answer{1/x}$$

$$J^{-1} = \frac{\partial(u,v)}{\partial(x,y)} = y\cdot\frac{1}{x} - x\cdot\left(-\frac{y}{x^2}\right) = \frac{y}{x}+\frac{y}{x} = \answer{2v}$$

Forward Jacobian: $J = \dfrac{1}{2v}$, so $|J| = \dfrac{1}{2v}$.

\textbf{Step 4: Area of the region.}
$$\text{Area} = \int_1^3\int_1^3 \frac{1}{2v}\,du\,dv = \int_1^3 \frac{1}{2v}\,dv \cdot \int_1^3 du = \frac{\ln 3}{2} \cdot 2 = \answer{\ln 3}$$

\begin{feedback}
When the forward transformation $(u,v)\to(x,y)$ is messy to differentiate, compute the inverse Jacobian $\partial(u,v)/\partial(x,y)$ instead and take the reciprocal.
\end{feedback}
\end{problem}

% --- Linearized Hyperbolic-Region Transformation ($u=xy$, $v=y/x$) ---
% T: (u,v) -> (x,y) with x=sqrt(u/v), y=sqrt(uv).  J = 1/(2v).
% Evaluated at (u_0,v_0)=(2,2): x_0=1, y_0=2.
%   e_u = (∂x/∂u, ∂y/∂u) = (1/(2√(uv)), √v/(2√u)) = (1/4, 1/2)  at (2,2)
%   e_v = (∂x/∂v, ∂y/∂v) = (-√u/(2v^{3/2}), √u/(2√v)) = (-1/4, 1/2) at (2,2)
% |J| = 1/(2v_0) = 1/4.
\begin{center}
\begin{tikzpicture}[scale=1.25]

% Column vectors of J at (u0,v0)=(2,2), scaled ×4 for visibility
\def\euxC{1.0}   \def\euyC{2.0}    % e_u = (1/4, 1/2)×4
\def\evxC{-1.0}  \def\evyC{2.0}    % e_v = (-1/4,1/2)×4

%=== LEFT: parameter (u,v) space ===
\begin{scope}[xshift=-0.3cm]
  \draw[->](-0.2,0)--(3.4,0) node[right]{$u$};
  \draw[->](0,-0.2)--(0,3.4) node[above]{$v$};
  % Hyperbolic region boundary lines (u=1,3 and v=1,3 are straight in uv-space)
  \fill[blue!10,opacity=0.6] (0.5,0.5) rectangle (1.5,1.5);
  \draw[blue!40!black,thick,dashed] (0.5,0) -- (0.5,3.2) node[above,font=\tiny]{$u=1$};
  \draw[blue!40!black,thick,dashed] (1.5,0) -- (1.5,3.2) node[above,font=\tiny]{$u=3$};
  \draw[blue!40!black,thick,dashed] (0,0.5) -- (3.2,0.5) node[right,font=\tiny]{$v=1$};
  \draw[blue!40!black,thick,dashed] (0,1.5) -- (3.2,1.5) node[right,font=\tiny]{$v=3$};

  % Highlighted cell centered near (u0,v0)=(2,2)
  \fill[blue!25,opacity=0.8] (0.9,0.9) rectangle (1.25,1.25);
  \draw[blue!60!black,thick] (0.9,0.9) rectangle (1.25,1.25);

  \draw[->,orange!80!black,very thick] (0.9,0.9) -- (1.25,0.9)
        node[midway,below,font=\small]{$\begin{bmatrix}\Delta u \\ 0\end{bmatrix}$};
  \draw[->,violet!80!black,very thick] (0.9,0.9) -- (0.9,1.25)
        node[midway,left,font=\small]{$\begin{bmatrix}0 \\ \Delta v\end{bmatrix}$};

  \fill[black] (0.9,0.9) circle (1.5pt);
  \node[font=\scriptsize,below right] at (0.9,0.9) {$(u_0,v_0)$};
  \node[font=\small,align=center,below] at (1.5,-0.45)
        {\textbf{Parameter space} $(u,v)$\\rectangle; $1\leq u\leq3$, $1\leq v\leq3$};
\end{scope}

%=== ARROW ===
\begin{scope}[xshift=3.6cm, yshift=1.5cm]
  \draw[->,very thick,gray!60](0,0)--(1.2,0) node[midway,above,font=\small]{$J$};
  \node[gray!60,font=\scriptsize,align=center] at (0.6,-0.38)
        {$|J|=\tfrac{1}{2v}$\\(varies)};
\end{scope}

%=== RIGHT: xy space — parallelogram (non-rectangular!) ===
\begin{scope}[xshift=5.4cm, yshift=-0.8cm]
  \draw[->](-0.3,0)--(2.8,0) node[right]{$x$};
  \draw[->](0,-0.3)--(0,3.4) node[above]{$y$};

  % Hyperbolic region boundaries in xy-space (sketched)
  % xy=1 (hyperbola), xy=3, y=x, y=3x
  \clip (-0.2,-0.2) rectangle (2.7,3.3);
  \draw[blue!40!black,thin,dashed,domain=0.15:2.6,samples=60]
        plot (\x, {1/\x}) node[right,font=\tiny]{$xy=1$};
  \draw[blue!40!black,thin,dashed,domain=0.5:2.6,samples=60]
        plot (\x, {3/\x}) node[right,font=\tiny]{$xy=3$};
  \draw[blue!40!black,thin,dashed,domain=0.1:2.1]
        plot (\x, \x) node[right,font=\tiny]{$y=x$};
  \draw[blue!40!black,thin,dashed,domain=0.1:1.0]
        plot (\x, {3*\x}) node[right,font=\tiny]{$y=3x$};

  % Parallelogram at base point T(u0,v0)=(x0,y0)=(1,2)
  \coordinate (P3) at (1.0, 2.0);
  % Scale column vectors by 0.25 for display (going from ×4 back to ×1)
  \pgfmathsetmacro{\euxCs}{\euxC*0.25}
  \pgfmathsetmacro{\euiCs}{\euyC*0.25}
  \pgfmathsetmacro{\evxCs}{\evxC*0.25}
  \pgfmathsetmacro{\evyCs}{\evyC*0.25}
  \coordinate (Q3) at ($(P3)+(\euxCs,\euiCs)$);
  \coordinate (R3) at ($(P3)+(\evxCs,\evyCs)$);
  \coordinate (S3) at ($(P3)+(\euxCs+\evxCs,\euiCs+\evyCs)$);

  \fill[blue!25,opacity=0.8] (P3)--(Q3)--(S3)--(R3)--cycle;
  \draw[blue!60!black,thick] (P3)--(Q3)--(S3)--(R3)--cycle;

  \draw[->,orange!80!black,very thick] (P3)--(Q3)
        node[midway,right,font=\footnotesize,xshift=2pt]
        {$\Delta u\begin{bmatrix}\tfrac{1}{4}\\\tfrac{1}{2}\end{bmatrix}$};
  \draw[->,violet!80!black,very thick] (P3)--(R3)
        node[midway,left,font=\footnotesize]
        {$\Delta v\begin{bmatrix}-\tfrac{1}{4}\\\tfrac{1}{2}\end{bmatrix}$};

  \fill[black] (P3) circle (1.5pt);
  \node[font=\scriptsize,right] at (P3) {\;$(x_0,y_0)$};

  % Area label
  \node[blue!70!black,font=\small] at ($(P3)+({(\euxCs+\evxCs)/2},{(\euiCs+\evyCs)/2})+(0.4,0)$)
        {Area $= \tfrac{1}{4}\Delta u\,\Delta v$};

  % Annotation box
  \draw[red!60!black,thick,rounded corners] (-0.2,-0.55) rectangle (2.65,-0.2);
  \node[red!60!black,font=\small] at (1.2,-0.37)
        {$|\det J|{=}\tfrac{1}{4}\cdot\tfrac{1}{2}-({-}\tfrac{1}{4})\cdot\tfrac{1}{2}{=}\tfrac{1}{4}$ at $(u_0,v_0){=}(2,2)$};

  \node[font=\small,align=center,below] at (1.2,-0.58)
        {\textbf{Linearized image in $(x,y)$}\\non-rectangular; $\vec{e}_u$ and $\vec{e}_v$ not perpendicular};
\end{scope}

\end{tikzpicture}
\end{center}

\subsection*{Example 4: Matching the Integrand}

\begin{problem}
Evaluate $\displaystyle\iint_R e^{(x-y)/(x+y)}\,dA$ where $R$ is the triangle with vertices $(0,0)$, $(2,0)$, $(0,2)$.

\textbf{Step 1: Choose the transformation.}

Let $u = x - y$ and $v = x + y$.  The function becomes $e^{u/v}$.

Identify the new bounds:
\begin{itemize}
  \item On $x+y = 2$ (hypotenuse): $v = \answer{2}$
  \item On $y = 0$: $u = x = v$, so $u = \answer{v}$
  \item On $x = 0$: $u = -y = -v$, so $u = \answer{-v}$
\end{itemize}

The region becomes:
$$0 \leq v \leq 2, \qquad \answer{-v} \leq u \leq \answer{v}$$

\textbf{Step 2: Solve for $x$ and $y$.}
$$x = \frac{u+v}{2}, \qquad y = \frac{v-u}{2}$$

\textbf{Step 3: Jacobian.}  (same calculation as Example 1):
$$|J| = \answer{1/2}$$

\textbf{Step 4: Evaluate.}
$$\iint_R e^{(x-y)/(x+y)}\,dA = \int_0^2\int_{-v}^{v} e^{u/v} \cdot \frac{1}{2}\,du\,dv$$

Inner integral:
$$\int_{-v}^{v} e^{u/v}\,du = \left[\answer{v\,e^{u/v}}\right]_{-v}^{v} = v\bigl(e - e^{-1}\bigr)$$

Full integral:
$$\frac{1}{2}\int_0^2 v\bigl(e - e^{-1}\bigr)\,dv = \frac{e-e^{-1}}{2}\cdot\Bigl[\frac{v^2}{2}\Bigr]_0^2 = \frac{e-e^{-1}}{2}\cdot 2 = \answer{e - 1/e}$$

\begin{feedback}
The choice $u = x-y$, $v = x+y$ was motivated entirely by the integrand: the exponent is $(x-y)/(x+y) = u/v$ in the new coordinates.  Good transformation choices make the integrand simple \emph{and} the region simple simultaneously.
\end{feedback}
\end{problem}

% --- Linearized Triangle Transformation ($u=x-y$, $v=x+y$) ---
% Same linear map as Example 1: e_u=(1/2,-1/2), e_v=(1/2,1/2), |J|=1/2.
% This time, the region in (u,v)-space is the triangle 0≤v≤2, -v≤u≤v.
\begin{center}
\begin{tikzpicture}[scale=1.25]

% Column vectors (same J as Ex.1, scaled ×2 for display)
\def\euxD{1.0}   \def\euyD{-1.0}   % e_u=(1/2,-1/2)×2
\def\evxD{1.0}   \def\evyD{1.0}    % e_v=(1/2, 1/2)×2

%=== LEFT: parameter (u,v) space — triangular region ===
\begin{scope}[xshift=-0.3cm]
  \draw[->](-1.6,0)--(1.8,0) node[right]{$u$};
  \draw[->](0,-0.2)--(0,2.8) node[above]{$v$};
  % Triangle: 0≤v≤2, -v≤u≤v  (in display units: scale by 0.9)
  \fill[blue!10,opacity=0.6] (0,0) -- (1.8,1.8) -- (-1.8,1.8) -- cycle;
  \draw[blue!50!black,thick,dashed] (0,0)--(1.8,1.8) node[right,font=\tiny]{$u=v$};
  \draw[blue!50!black,thick,dashed] (0,0)--(-1.8,1.8) node[left,font=\tiny]{$u=-v$};
  \draw[blue!50!black,thick,dashed] (-1.8,1.8)--(1.8,1.8) node[right,font=\tiny]{$v=2$};

  % Small highlighted cell
  \fill[blue!25,opacity=0.8] (0.3,1.1) rectangle (0.7,1.5);
  \draw[blue!60!black,thick] (0.3,1.1) rectangle (0.7,1.5);

  \draw[->,orange!80!black,very thick] (0.3,1.1) -- (0.7,1.1)
        node[midway,below,font=\small]{$\begin{bmatrix}\Delta u \\ 0\end{bmatrix}$};
  \draw[->,violet!80!black,very thick] (0.3,1.1) -- (0.3,1.5)
        node[midway,left,font=\small]{$\begin{bmatrix}0 \\ \Delta v\end{bmatrix}$};

  \fill[black] (0.3,1.1) circle (1.5pt);
  \node[font=\scriptsize,right] at (0.3,1.1) {\;$(u_0,v_0)$};

  \node[font=\small,align=center,below] at (0,-0.45)
        {\textbf{Parameter space} $(u,v)$\\triangular region};
\end{scope}

%=== ARROW ===
\begin{scope}[xshift=2.8cm, yshift=0.8cm]
  \draw[->,very thick,gray!60](0,0)--(1.2,0) node[midway,above,font=\small]{$J$};
  \node[gray!60,font=\scriptsize,align=center] at (0.6,-0.35){$J=\tfrac{1}{2}$\\(constant)};
\end{scope}

%=== RIGHT: xy space — triangle + parallelogram ===
\begin{scope}[xshift=5.0cm, yshift=0cm]
  \draw[->](-0.3,0)--(2.8,0) node[right]{$x$};
  \draw[->](0,-1.3)--(0,2.4) node[above]{$y$};
  % Image of triangle: vertices (0,0),(2,0),(0,2)
  \fill[blue!10,opacity=0.5] (0,0)--(2,0)--(0,2)--cycle;
  \draw[blue!50!black,thick,dashed] (0,0)--(2,0) node[right,font=\tiny]{$y=0$};
  \draw[blue!50!black,thick,dashed] (0,0)--(0,2) node[above,font=\tiny]{$x=0$};
  \draw[blue!50!black,thick,dashed] (2,0)--(0,2) node[midway,above right,font=\tiny]{$x+y=2$};

  % Parallelogram at T(u0,v0)
  \coordinate (P4) at (1.0, 0.2);
  \pgfmathsetmacro{\euxDs}{\euxD*0.25}  \pgfmathsetmacro{\euyDs}{\euyD*0.25}
  \pgfmathsetmacro{\evxDs}{\evxD*0.25}  \pgfmathsetmacro{\evyDs}{\evyD*0.25}
  \coordinate (Q4) at ($(P4)+(\euxDs,\euyDs)$);
  \coordinate (R4) at ($(P4)+(\evxDs,\evyDs)$);
  \coordinate (S4) at ($(P4)+(\euxDs+\evxDs,\euyDs+\evyDs)$);

  \fill[blue!30,opacity=0.7] (P4)--(Q4)--(S4)--(R4)--cycle;
  \draw[blue!60!black,thick] (P4)--(Q4)--(S4)--(R4)--cycle;

  \draw[->,orange!80!black,very thick] (P4)--(Q4)
        node[midway,below right,font=\footnotesize]
        {$\Delta u\begin{bmatrix}\tfrac{1}{2}\\-\tfrac{1}{2}\end{bmatrix}$};
  \draw[->,violet!80!black,very thick] (P4)--(R4)
        node[midway,left,font=\footnotesize]
        {$\Delta v\begin{bmatrix}\tfrac{1}{2}\\\tfrac{1}{2}\end{bmatrix}$};

  % Right-angle marker
  \pgfmathsetmacro{\nEuD}{sqrt(\euxDs*\euxDs+\euyDs*\euyDs)}
  \pgfmathsetmacro{\uuDx}{\euxDs/\nEuD*0.18} \pgfmathsetmacro{\uuDy}{\euyDs/\nEuD*0.18}
  \pgfmathsetmacro{\nEvD}{sqrt(\evxDs*\evxDs+\evyDs*\evyDs)}
  \pgfmathsetmacro{\uvDx}{\evxDs/\nEvD*0.18} \pgfmathsetmacro{\uvDy}{\evyDs/\nEvD*0.18}
  \draw[gray!60,thin]
    ($(P4)+(\uuDx,\uuDy)$)--($(P4)+(\uuDx+\uvDx,\uuDy+\uvDy)$)--($(P4)+(\uvDx,\uvDy)$);

  \fill[black] (P4) circle (1.5pt);

  % Area label
  \node[blue!70!black,font=\small] at ($(P4)+({(\euxDs+\evxDs)/2},{(\euyDs+\evyDs)/2})+(0.45,0)$)
        {$\tfrac{1}{2}\Delta u\,\Delta v$};

  % Annotation box
  \draw[red!60!black,thick,rounded corners] (-0.2,-1.2) rectangle (2.65,-0.85);
  \node[red!60!black,font=\small] at (1.2,-1.02)
        {$|\det J|=\tfrac{1}{2}$ (same linear map as Example~1)};

  \node[font=\small,align=center,below] at (1.2,-1.25)
        {\textbf{Linearized image in $(x,y)$}\\rhombus; image triangle has vertices $(0,0),(2,0),(0,2)$};
\end{scope}

\end{tikzpicture}
\end{center}

\end{document}
